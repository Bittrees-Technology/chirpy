{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-update",
  "description": "Desktop auto-update: check/download/install updates and relaunch.",
  "platforms": ["macOS", "windows", "linux"],
  "windows": ["main"],
  "permissions": [
    "updater:default",
    "process:allow-restart"
  ]
}
