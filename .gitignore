node_modules/
dist/
build/
.DS_Store
*.log
.env
.env.*
!.env.example
# Self-hosted gate secrets (holds the gatekeeper private key)
selfhost/gate.env
.turbo/
# Tauri native build artifacts
apps/web/src-tauri/target/
apps/web/src-tauri/gen/
# Tauri updater signing key — NEVER commit the private key
apps/web/src-tauri/.tauri/
.vercel
uploads/

# Playwright
test-results/
playwright-report/

# Operator-reviewed local gate registry
selfhost/rooms.json

# Private wallet-email service configuration and verification records
selfhost/mail.env
selfhost/mail-bindings.json

.push-runtime-dist/

.push-ui-dist/
