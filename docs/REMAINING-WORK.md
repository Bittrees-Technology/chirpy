# Remaining work before Chat launch

Use [PRODUCTION-READINESS.md](PRODUCTION-READINESS.md) for completed work and evidence. This list consolidates unfinished review and unification work as of 23 September 2026. Chat serves on both Chat and Chirpy domains, but consumer cutover, default Chirpy forwarding and GitHub/Vercel renaming remain gated by combined launch acceptance. Passing source checks does not establish production service, device or operator acceptance.

## Production release dependencies

1. **Gate host and identity:** choose/provision the host and TLS domain, persistent production wallet/database keys, key custody, production RPC, reviewed nonempty registry and bot super-admin roles. Set matching browser, web-health and organization routing values; opt into native origins where required.
2. **Real admission acceptance:** qualifying and denied wallets against actual on-chain rules, inbox substitution/replay rejection, restart continuity, multi-wallet binding and multiple devices. Synthetic dev-network drills do not replace this.
3. **Operations:** backup scheduling, independently controlled key backups, operator restore and measured recovery targets; alert destinations, incident owner and escalation process. Review membership audit outcomes before enforcement. Complete a moderation/reporting process and any required shared abuse controls.
4. **Public policies:** identify the operator, actual retention/deletion rules and contacts, then publish approved privacy/terms. Support/security paths exist; no invented legal entity, retention period or response-time promise should fill this gap.
5. **Native distribution:** supply Apple/platform signing credentials and repository variables (the updater key is verified and configured); verify signed/notarized macOS and Windows signing, downloaded artifact signatures, clean install and updater behavior. Complete signed device packaging and iOS real-device storage/wallet-return/deep-link and TestFlight acceptance. Prepared artifacts remain drafts.
6. **Dependency follow-up:** resolve or explicitly accept applicable native/container findings. The 9 September baselines recorded seven native warnings and 20 unfixed medium/low container findings; use the current CI reports when making a release decision. [Native platform applicability](security/NATIVE-DEPENDENCIES.md) is reviewed; track upstream fixes and recheck after dependency changes; no warning suppression or certification is implied by the audit gates.

## Product and integration completion

7. **Multi-device longevity:** open XMTP-room member additions have an administrator UI and fresh authority checks; group authors/rosters resolve SDK wallet identities for public labels, and creator/invited-member history recovery is tested on dev. Native group invitation acceptance/block/unblock controls are implemented with consent and session guards; native leave requests now distinguish pending and confirmed removal while retaining saved history/drafts; separate confirmed owner appointment and step-down are implemented for open native rooms; operator-managed gated ownership, production-origin/physical-device acceptance of the implemented linked-wallet display preference (signed public choice, current SDK link validation, expiring display resolution and disposable-dev coverage; see [DISPLAY-WALLET.md](DISPLAY-WALLET.md)), actual production group membership/history and physical-device acceptance remain. Complete extended offline/reconnect/conflict/revocation testing on real devices. Explicit deletion/unblock/reset merge semantics, opt-in client migration and encrypted recovery preservation are implemented and exercised in isolated two-device browsers. Current XMTP blocking uses conversation consent, not the legacy blocked-address preference array. Unsupported encrypted payloads now pause this client’s writes and preserve data; the server format floor and isolated upgraded storage slot are implemented (see [SYNC-FORMAT-UPGRADE.md](SYNC-FORMAT-UPGRADE.md)); deployed Chirpy/Chat desktop-browser settings upgrade, deletion/unblock, offline merge and revocation passed with a disposable wallet on23September; native/physical-device and broader archive/history recovery acceptance remain.
8. **Scale and accessibility:** reduce initial/periodic full SDK synchronization and local full-list enumeration costs, complete real-device large-inbox benchmarks and wider mobile/assistive-technology acceptance. Email reading/composition focus and accessible row selection are implemented with browser checks for delayed responses, polling and cancellation. Rendering/history/profile work and application-level concurrency are bounded; streamed updates refresh affected summaries, healthy-stream polling is now once per minute, and streamed/local changes read up to 100 known affected SDK records directly (at most eight reads in flight); unknown/missing records, larger bursts and unscoped refreshes enumerate locally. Initial and periodic reconciliation still synchronize and list the complete inbox. English/Spanish catalogs cover the current referenced UI keys, and the remaining known wallet/sync/ENS status messages are translated. Third-party diagnostics remain verbatim; native-language review is still required.
9. **Governance/Research integration:** core Push source selection, history, membership/moderation and text/file/reply composition (up to six files per standalone message), bounded PNG/JPEG previews and native add-only reactions are implemented, preserving original protocol IDs. Complete existing private-room history/member/action acceptance, legacy identities, physical devices and remaining composite/media parity and protocol limitations on reaction removal. Confirm Research’s deployed registry changes against main before its export/provider hardening; retain legacy access, including discovery of any historical Research rooms. Governance encrypted export/Chat local restore passed on deployed origins in isolated desktop Chromium, including separate preference consent, backup and undo; complete Research and physical/native-device acceptance. No consumer cutover has shipped; see [INTEGRATION.md](INTEGRATION.md). Shared UI packaging and iframe embedding are optional later work, not substitutes for the chosen standalone-app migration.
10. **Additional resolvers and presets:** trusted role, voting-power, Safe-delegate and other-chain sources; verified real preset contracts and broader ERC-1155 discovery if required. Unsupported production rules remain disabled. Protocol-wide posting freezes require an enforcement mechanism beyond the current advisory client policy.
11. **Artwork:** replace interim icons with approved final brand assets and verify native/export sizes.

12. **Connected Mail acceptance:** inbox/folders/conversations, read/send/reply, duration dropdown including Until revoked, formatted previews and bounded attachments are implemented. The approved self-addressed browser tests passed, including a byte-exact 256 KiB file round trip and complete-file download. Complete contract-wallet/native handoffs, account-change/natural-expiry/device tests, live paging/rich-mail acceptance, the new 1 MiB transfer contract on actual accounts/devices and files above 1 MiB; see [CONNECTED-MAIL.md](CONNECTED-MAIL.md). General third-party mailbox providers are a later expansion.
13. **Verified wallet↔email forwarding:** deploy the Wallet verification/consent service, configure production host/domain/credentials and enroll approved bindings. Outbound queue/authority, provider events/suppression and opt-out are implemented but require live provider/Mail-backed delivery, webhooks, scheduler and approved real-account acceptance. Inbound source outbox, encrypted receiver, dedicated sender, one-attempt journal, queue worker, health and encrypted backup/provisioning tools are implemented; the separate Acer source/runtime is installed disabled. Production sender custody, protected SDK storage, fresh authority/enrollment, scheduler/alerts, production restore/reconciliation and actual delivery in both directions remain. Signed outbound receipt details, session history and wallet-signed lost-ID discovery with explicit local restore are implemented; provider delivery-event reconciliation is implemented with separate bounded facts and signed receipt lookup; production webhook configuration/replay acceptance, legacy indexing/discovery acceptance and broader abuse controls remain. Incoming history is implemented for newly tagged receipts; live original-recipient ownership, legacy coverage and encryption-key rotation acceptance remain. No live forwarding is enabled; see [WALLET-EMAIL.md](WALLET-EMAIL.md) and [INBOUND-MAIL.md](INBOUND-MAIL.md).
14. **Profile and recovery acceptance:** public profile choice/withdrawal and private-name encrypted recovery are implemented. Complete physical/native and actual cross-origin recovery acceptance, operator retention/abuse policy and rollout of the recovery-capable client. Keep internal service identities, database keys, signatures and native update identifiers stable.
15. **Final cutover:** complete the combined acceptance matrix, consumer entry-point changes and rollback proof before default Chirpy forwarding. Rename the GitHub repository and Vercel project to `chat` after verification; recheck integrations, deployment hooks and native/update links afterward.

## Later expansion

Voice calls, presence/SSE and optional organization relays follow core release acceptance. They are not implemented by the security hardening increments.

Offline preparation: `node scripts/mail-preflight.mjs` now checks mail and webhook configuration without network access or secret output. Passing it does not satisfy the external activation and acceptance dependencies above.


SDK follow-up: replace the pinned local XMTP consent patch only after a compatible upstream release passes its timestamp/default/no-op regressions and real two-installation acceptance. Native group accept/block and readable inactive archived history are implemented, with actions disabled until active access is confirmed. Complete production-origin and physical-device recovery acceptance; blocked status alone is not proof of removed membership. Native leave requests preserve pending versus completed membership, disable actions while pending, and retain readable local history. Completion requires a leave request observed by the current client plus inactive access and a roster without self; incomplete restored membership alone is never treated as removal. Reloaded or restored clients without that evidence retain the conservative inactive-access notice. Open native room owners can explicitly appoint an existing member, then separately step down and leave. Fresh wallet, namespace, consent, membership and owner checks protect each dispatch; the final owner cannot step down. Promotion retries are idempotent, and interrupted requests require checking current roles. Directory/gated ownership remains with its operator. The browser SDK worker adapts requestRemoval to leaveGroup and reads PendingRemove membership state; the differing public names are compatible. Upstream rejects leave for every super-admin until demoted, so owners must use the separate ownership controls before leaving. Disposable two-wallet dev acceptance verifies singleton/super-admin/DM restrictions, cancellation, offline-owner pending requests, and confirmed removal after the owner reconnects. Existing installations must receive the corrected SDK through the applicable web/native rollout.
